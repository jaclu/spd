# Task specific config

Not auto parsed, the task must parse the task config by itself.
Please remember to parse global_overrides.yml after the task config is parsed,
to ensure operator overrides are honored!
