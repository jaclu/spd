# Description tasks

Here task specific overrides can be located, they will be read just as the task
sarts, so will override any generic settings

Not meant to change the tasks default config if any, rahther the intent is to handle
if a generic setting needs to be altered specifically for a task.
