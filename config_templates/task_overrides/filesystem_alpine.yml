---
# Primary focus wouldn't be to override the package selection in SPD_APK_INSTALL etc
# that can be done directly in the configs/file_systems/alpine.yml file,
# but to potentially change other setings that should be altered for this specific task

# SPD_PKGS_MAN: true # If set to 1 man pages will be deployed
# SPD_PKGS_DEVEL: yes # If set to 1 development packages will be deployed
# SPD_PKGS_LINTING: 1 # If set to 1 linting packages will be deployed

SPD_APK_INSTALL:
  - bash # The GNU Bourne Again shell
  - bash-completion # Command-line tab-completion for bash
  - openrc
  - mg
  - ripgrep
  - autossh

SPD_PKGS_MAN: 0 # If set to 1 man pages will be deployed
SPD_PKGS_DEVEL: 0 # If set to 1 development packages will be deployed
SPD_PKGS_LINTING: 0 # If set to 1 linting packages will be deployed
